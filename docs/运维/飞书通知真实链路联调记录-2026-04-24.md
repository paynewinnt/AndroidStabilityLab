# 飞书通知真实链路联调记录 - 2026-04-24

## 1. 目标

本记录用于沉淀 `Android Stability Lab` 接入飞书自定义机器人的真实链路状态，覆盖：

- 飞书 webhook 注册状态
- canary 事件状态
- 首轮真实投递结果
- 后续 2 小时 / 24 小时联调准入条件

本文档不记录飞书 `signing_secret` 明文。

## 2. 当前配置

- Webhook 名称：`Android Stability Lab`
- Delivery channel：`feishu_bot`
- URL：`https://open.feishu.cn/open-apis/...`
- 订阅事件：`admission_case.updated`、`admission.override_recorded`、`outbox.retry_alert`
- Signature key id：`feishu-v1`
- Failure policy：`retryable_http`

当前本机已注册 webhook，配置保存在 `runtime/integration_outbox/webhooks.json`。该文件包含本地运行密钥配置，不应提交到仓库。

## 3. Canary 事件

- Event id：`outbox_event_971620306bbb4b8c8f3cb42808bbd081`
- Event type：`admission_case.updated`
- Target：`admission_case:feishu_canary_20260424`
- 当前状态：`pending`
- Attempt count：`0`

说明：canary 已写入 outbox，但尚未完成真实飞书投递。

## 4. 已完成验证

- `register-feishu-webhook` 已可注册飞书机器人出口。
- `run-feishu-notify-worker --help` 可用。
- 飞书通道单测通过，覆盖 `timestamp / sign / msg_type / content`、consumer receipt 和 worker 入口。
- 本地 IM mock/smoke 已通过，可证明平台侧 outbox、worker、签名、幂等和 receipt 机制可用。

## 5. 真实投递状态

首次在默认 sandbox 内执行真实飞书 worker 时，历史事件出现 DNS/网络层失败：

- Failure category：`transport_error`
- Error 摘要：`nodename nor servname provided, or not known`

随后尝试用提升权限访问真实飞书 webhook 被安全审查拒绝，因为该动作会把本地 outbox/admission payload 发往第三方飞书 webhook。

因此，本轮没有完成真实飞书消息送达，也不能标记为 2 小时或 24 小时联调通过。

## 6. 后续执行命令

若已确认允许把本地 outbox/admission payload 发送到该飞书机器人，可在本机终端执行以下命令。

执行前从本机环境变量确认真实配置，不在文档中记录明文：

```bash
export FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/<token>"
export FEISHU_SIGNING_SECRET="<feishu-bot-secret>"
```

单轮 canary：

```bash
./.venv/bin/python -m stability.cli run-feishu-notify-worker \
  --webhook-name "Android Stability Lab" \
  --limit-per-webhook 30 \
  --interval-seconds 0 \
  --max-rounds 1 \
  --stop-when-idle
```

2 小时试运行：

```bash
./.venv/bin/python -m stability.cli run-feishu-notify-worker \
  --daemon \
  --webhook-name "Android Stability Lab" \
  --interval-seconds 60 \
  --limit-per-webhook 20 \
  --max-runtime-seconds 7200
```

24 小时长时联调：

```bash
./.venv/bin/python -m stability.cli run-feishu-notify-worker \
  --daemon \
  --webhook-name "Android Stability Lab" \
  --interval-seconds 300 \
  --limit-per-webhook 50 \
  --max-runtime-seconds 86400
```

## 7. 验收记录模板

真实联调完成后补齐：

- 开始时间：
- 结束时间：
- 实际运行时长：
- Worker 参数：
- 总 attempted：
- 总 delivered：
- 总 failed：
- Dead-letter 数：
- Replay 次数：
- remaining_pending 趋势：
- 飞书群是否收到消息：
- 飞书签名是否通过：
- consumer receipt 是否可反查：
- idempotency key 是否可反查：
- dead-letter replay receipt 是否可反查：
- 是否存在关键词拦截：
- 是否存在限流：
- 噪声反馈：`可接受 / 偏高 / 不可接受`
- 2 小时试运行结论：`通过，可进入 24 小时联调 / 条件通过 / 不通过 / 未执行`
- 24 小时联调结论：`通过 / 条件通过 / 不通过 / 未执行`
- 最终结论：`平台侧能力通过 / 真实 24 小时通过 / 条件通过 / 不通过`

更完整的可复制模板见 `docs/运维/飞书IM真实链路2小时与24小时验收记录模板.md`。

## 8. 风险

- 当前密钥保存在本地 `runtime/`，不要提交仓库。
- 如果飞书机器人开启关键词校验，需要确保消息文本包含关键词。
- 当前飞书消息为 `text`，不是交互卡片。
- 本记录只证明配置和 canary 已准备好；真实送达仍待人工授权或本机终端执行。
- 截至本记录，2 小时试运行和 24 小时长时联调均未完成，因此不能把飞书 IM 真实链路写成完成项。
